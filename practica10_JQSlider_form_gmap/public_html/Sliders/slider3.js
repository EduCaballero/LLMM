$(document).ready(ini);
function ini() {
    $('.single-item').slick({
        autoplay: true,
        dots:true
    });
}

$(document).ready(ini);
    function ini() {
    $('.multiple-items').slick({
      infinite: true,
      slidesToShow: 2,
      autoplay:true  
    });
}